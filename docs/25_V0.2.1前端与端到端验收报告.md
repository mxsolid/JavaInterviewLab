# 25 V0.2.1 前端与端到端验收报告

日期：2026-08-20。

## 运行地址与健康

- 浏览器：`http://127.0.0.1:5173`
- 后端：`http://127.0.0.1:8080`
- 健康检查：`GET /actuator/health` 返回 `UP`。
- Flyway：本地 PostgreSQL 16 `devdb` 已验证 11 个 migration，schema 版本为 10。

## Seed 与页面

- 本地数据库已有 `v01-core` 与 B08 验收数据；本次未重复导入 Seed。
- Dashboard：当前路线、已练习、较熟练及以上、待复习、错题、最近学习均从 API 读取；快捷入口可跳转。
- StudyPlan：10/15/30 天路线、今日任务、专题/题目跳转保留；SCENARIO 不会误跳题库。
- QuestionBank：筛选、分页、编辑保留，增加统一标题、卡片和中文枚举显示。
- QuestionDetail：默认练习模式不会直接展示答案；学习模式提供说明和回答 Tab。
- Review：`/review` 包含待复习、错题本、收藏 Tab，逾期任务按 `overdue` 显示。

## Practice 与数据链

浏览器实际打开 `questionId=26`，输入回答、查看参考答案、选择“回答错误”并提交。页面返回“本次练习已保存”，显示练习次数 2、错误次数 2、下次复习时间。

随后读取 `GET /api/study/questions/26/progress`，确认 `attemptCount=2`、`wrongCount=2`、`wrongBookActive=true`。同 UUID 的 API 重试检查从 `attemptCount=2` 到 `3` 只增加一次，第二次响应 `duplicated=true` 且回读相同 attempt。

## Note

浏览器修改 questionId 26 的笔记，约 1 秒后显示“已保存”。刷新页面后仍回读 `B08重启验收笔记；FE09 串行自动保存验收。`。

## 构建与测试

- `npm ci`：通过。
- `npm run typecheck`：通过。
- `npm run build`：通过。Vite 报告既有单主包超过 500 KB 警告。
- `mvn -B -ntp clean test`：通过，25 个测试。
- `mvn -B -ntp package -DskipTests`：通过。

## 已知问题

- 当前生产主包 1.24 MB，Vite 给出超过 500 KB 的拆包提示；本轮未为此引入无关的路由拆包改造。
- Ant Design 6 对现有 `Space direction` 与 `List` 给出弃用警告，不影响本轮接口联调和页面功能；后续升级组件 API 时应统一处理。
