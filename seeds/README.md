# Seed 题库说明

这里的 JSON 是“内容模型示范”，不是最终大型题库。

开发 A11 时实现 SeedImporter：
- 读取 seedPack；
- externalKey 唯一；
- 支持版本；
- 事务导入；
- 重复执行幂等；
- 导入失败整批回滚；
- 输出导入统计。

后续扩题时：
- 每批 10～30 个高质量主题；
- Review 后再合并；
- 不建议 Agent 一次生成数百道题。
